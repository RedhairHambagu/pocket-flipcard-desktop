{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
